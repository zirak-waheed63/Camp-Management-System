<div id="msform">
  <ul id="progressbar">
    <% @camp_application.steps.keys.each do |key| %>
      <li class= "<%= 'active' if @camp_application.steps[key].present? %> <%= 'current' if current_step == key %>" ><%= link_to key.humanize, admin_camp_application_path(application_id: @camp_application.id, id: key) %></li>
    <% end %>
  </ul>
</div>
