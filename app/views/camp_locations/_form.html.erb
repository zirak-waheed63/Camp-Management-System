<%= form_with(model: camp_location, local: true) do |form| %>
  <% if camp_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(camp_location.errors.count, "error") %> prohibited this camp_location from being saved:</h2>

      <ul>
      <% camp_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
